## Instant Installation
Run this single command to download Enumspire and install it globally. 

```bash
sudo git clone https://github.com/RaadShkakhwa1/enumspire.git /opt/enumspire && sudo ln -s /opt/enumspire/src/enumspire.py /usr/local/bin/enumspire
