## Instant Installation
Run this single command to download Enumspire and install it globally. 

```bash
git clone https://github.com/RaadShkakhwa1/enumspire.git /opt/enumspire && sudo bash /opt/enumspire/install.sh
